build:
  roots:
    - .
lint:
  use:
    - DEFAULT
    - FILE_LOWER_SNAKE_CASE
    - COMMENT_MESSAGE
  except:
    - ENUM_NO_ALLOW_ALIAS
    - PACKAGE_AFFINITY
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_SAME_DIRECTORY
breaking:
  use:
    - WIRE_JSON
